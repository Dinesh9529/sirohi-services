function selectUPI(provider) {
    const upiIds = {
        'gpay': 'serviceportal@gpay',
        'phonepe': 'serviceportal@phonepe',
        'paytm': 'serviceportal@paytm'
    };
    
    document.getElementById('upi-id').value = upiIds[provider];
}

function processPayment() {
    const upiId = document.getElementById('upi-id').value;
    
    if (!upiId.includes('@')) {
        alert('????? ??? UPI ID ???? ????');
        return;
    }
    
    // ?????? ?????????? (???? ?? ???)
    alert(`?1999 ?? ?????? ${upiId} ?? ??????? ???? ?? ??? ??...`);
    
    // ?????? ????? (???? ?? ???)
    setTimeout(() => {
        alert('?????? ???! ?? ?? ???????? ????? ????');
        window.location.href = 'dashboard.html';
    }, 2000);
}